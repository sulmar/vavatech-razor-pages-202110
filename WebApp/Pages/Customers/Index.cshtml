@page "/sklep/klienci"
@model WebApp.Pages.Customers.IndexModel
@{
}


<table class="table table-hover table-striped table-dark">
    <thead>
        <tr>
            <th>Imię</th>
            <th>Nazwisko</th>
            <th>Płeć</th>
            <td>Wynagrodzenie</td>
            <td>Grupa klientów</td>
            <td>Usunięty</td>
        </tr>
    </thead>

    <tbody>

        @foreach (var customer in Model.Customers)
        {
            <tr>
                <td>@customer.FirstName</td>
                <td>@customer.LastName</td>
                <td>@Html.DisplayFor(c => customer.Gender)</td>
                <td>@customer.Salary</td>
                <td>
                    @if (customer.CustomerGroup != null)
                    {
                        @customer.CustomerGroup.Name
                    }                    
                </td>
                <td>@customer.IsRemoved</td>
                <td>
                    <div>
                        <a class="btn btn-primary" asp-page="./Details" asp-route-id="@customer.Id">Podgląd</a>
                        <a class="btn btn-primary" asp-page="./Edit" asp-route-id="@customer.Id">Edycja</a>
                    </div>
                </td>
            </tr>

        }


    </tbody>
</table>
